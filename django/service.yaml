kind: Service
apiVersion: v1
metadata:
  name: peoplefinder-service
  namespace: peoplefinder
spec:
  selector:
    app: peoplefinder-container
  ports:
  - name: django
    protocol: TCP
    port: 80
    targetPort: 80
  - name: nginx
    protocal: TCP
    port: 80
    targetPort: 80
  type: NodePort
